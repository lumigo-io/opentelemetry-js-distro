{
  "name": "lumigo-prisma-postgres-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rm -rf prisma/migrations",
    "prep_db": "npm run clean && prisma generate && prisma db push",
    "start": "npm run prep_db && node -r @lumigo/opentelemetry prisma_postgres_app.js && npm run clean"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@lumigo/opentelemetry": "file:../../../../distro.tgz",
    "@prisma/client": "^4.2.0",
    "prisma": "^4.2.0"
  }
}
